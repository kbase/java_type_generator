package us.kbase.test5;

import java.util.Arrays;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

import junit.framework.Assert;

import us.kbase.test5.booltest.BooltestClient;

public class Test5 {
	
	public Test5(BooltestClient client) throws Exception {
		try {
			Boolean ret1 = client.boolCheck(true);
			Assert.assertTrue(ret1);
		} catch (Exception ex) {
			String msg = ex.getCause() == null ? ex.getMessage() : ex.getCause().getMessage();
			System.out.println("Booleans are not supported in spec-parser yet:");
			System.out.println(msg);
		}
	}
}